<section class="pt-5 text-center container">
  <div class="row py-lg-5">
    <div class="col-lg-6 col-md-8 mx-auto">
      <h3 class="fw-light">シェアハウス情報</h3>
      <p class="lead text-muted">シェアハウスに登録した情報の確認・編集が可能です。パスワードを変更する際はトラブルの原因となる可能性があるため、他のルームメイトの許可を得てから変更してください。</p>
      <p><%= link_to "メニューに戻る", root_path, class: "btn btn-outline-warning my-2" %></p>
    </div>
  </div>
</section>
<div class="card col-sm-6 mx-auto mb-5">
  <div class="card-body text-center mx-auto">
    <h4 class="mt-1 mb-3">編集</h4><br>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label "名前（シェアハウス）" %><br>
        <%= f.text_field :house_name, autocomplete: "off" %>
      </div>

      <div class="form-group">
        <%= f.label "住所（シェアハウス）" %><br>
        <%= f.text_field :address, autocomplete: "off" %>
      </div>

      <div class="form-group">
        <%= f.label "新規パスワード（6文字以上）" %><br>
        <%= f.password_field :password, autocomplete: "off" %>
      </div>

      <div class="form-group">
        <%= f.label "新規パスワード（確認用）" %><br>
        <%= f.password_field :password_confirmation, autocomplete: "off" %>
      </div>

      <div class="form-group">
        <%= f.label "変更前パスワード（必須）" %><br>
        <%= f.password_field :current_password, autocomplete: "off" %>
      </div>

      <div class="actions">
        <%= f.submit "変更", :class => "btn btn-outline-dark disabled" %>
      </div>

    <% end %>
  </div>
</div>