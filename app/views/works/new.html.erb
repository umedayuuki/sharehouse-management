<section class="py-5 text-center container">
  <div class="row py-lg-5">
    <div class="col-lg-6 col-md-8 mx-auto">
      <h3 class="fw-light">ワーク登録</h3>
      <p class="lead text-muted">役割の登録が可能です。</p>
      <p>
        <%= link_to "一覧に戻る", works_path, class: "btn btn-outline-warning my-2" %>
      </p>
    </div>
  </div>
</section>
<div class="card col-sm-6 mx-auto mb-5">
  <div class="card-body text-center mx-auto">
    <h4 class="mt-1 mb-3">ワーク内容</h4><br>
    <%= form_with(model: @work, local: true) do |form| %>
     <%= render 'layouts/error_messages', model: form.object %>

      <div class="form-group">
        <%= form.select :user_name, @users.map{ |user| user.user_name }, include_blank: "担当者を選択" %>
      </div>

      <div class="form-group">
        <%= form.text_field :work_name, autocomplete: "off" %>
      </div>

      <div class="actions">
        <%= form.submit "登録", :class => "btn btn-outline-dark disabled" %>
      </div>

    <% end %>
  </div>
</div>