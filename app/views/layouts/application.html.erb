<!DOCTYPE html>
<html>
  <head>
    <title>SharehouseManagement</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h2 class="grid-6"><a href="/">Sharehouse-Management</a></h2>
        <% if house_signed_in? %>
              <%= current_house.house_name %>でログインしています
              <%= link_to "ログアウト", destroy_house_session_path, method: :delete %>
          </div>
       <% else %>
          <div class="grid-6">
            <%= link_to "新規登録", new_house_registration_path, class: "post" %>
            <%= link_to "ログイン", new_house_session_path, class: "post" %>
          </div>
       <% end %>
      </div>
    </header>
    <%= yield %>
    <footer>
      <p>
        Sharehouse-Management 2021
      </p>
    </footer>
  </body>
</html>
