<!DOCTYPE html>
<html>
  <head>
    <title>シェアハウス管理アプリ</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><a href="/">シェアハウス管理アプリ</a></h1>
        <% if house_signed_in? %>
              <%= current_house.house_name %>でログイン中
              <%= link_to "ログアウト", destroy_house_session_path, method: :delete %>
          </div>
       <% else %>
          <div class="grid-6">
            <%= link_to "新規登録", new_house_registration_path, class: "post" %>
            <%= link_to "ログイン", new_house_session_path, class: "post" %>
          </div>
       <% end %>
      </div>
    </header>
    <%= yield %>
    <footer>
      <p>
        Sharehouse-Management 2021
      </p>
    </footer>
  </body>
</html>
